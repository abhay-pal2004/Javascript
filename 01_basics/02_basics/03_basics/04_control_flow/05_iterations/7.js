const myNumbers = [1, 2, 3, 4, 5]

// const myNums = myNumbers.map( (num) => {
//     return num + 9})
//     console.log(myNums);
 const newNums = myNumers.map( (num) => num * 10).map()
 console.log(newNums);